document.addEventListener('DOMContentLoaded', function() {
    var modalAddPosition = new bootstrap.Modal(document.getElementById('addPosition'));
    console.log(modalAddPosition);
    modalAddPosition.show();
});